package de.maifii.skywarsffa.listeners.game;


/*
public class InventoryChangeListener implements Listener {


    /*
    TODO: -diffrent if check / Event
          -OR set inventory in KitInvListener (X) [look up KitInvListener]


    */
/*
    @EventHandler
    public void onMove(PlayerMoveEvent event) {
        Player spieler = event.getPlayer();
        InventoryUtils inventoryUtils = new InventoryUtils();

        if(spieler.getLocation().getY() <= getInstance().getLocation().getDouble("Spawnheight.Y")) {
            inventoryUtils.setInventory(spieler);
        }
    }
}
 */
